// @fontsource packages are CSS-only and ship no type declarations for their
// side-effect entry points. Declare them so the imports in main.tsx typecheck.
declare module '@fontsource-variable/fraunces';
declare module '@fontsource-variable/nunito-sans';
