import { PageStub } from '@/components/layout/PageStub';

export function BoardPage() {
  return (
    <PageStub
      eyebrow="Your pipeline"
      title="Track every role through the funnel"
      description="A Kanban board to move each match along as you apply and interview."
      planned="Draggable job cards across columns — Matched, Shortlisted, Applied, Interviewing, Offer, Rejected — each with a score + company and a per-column count."
    />
  );
}
