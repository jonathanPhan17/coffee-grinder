import { PageStub } from '@/components/layout/PageStub';

export function ResultsPage() {
  return (
    <PageStub
      eyebrow="Your matches"
      title="The matches, best fit first"
      description="The core screen: scored job cards sorted highest-first, each with a fit ring, summary, and quick actions."
      planned="Sorted match cards (ScoreRing + title/company + one-line summary + View scorecard / Apply), filter & sort controls, plus skeleton-loading and empty states."
    />
  );
}
