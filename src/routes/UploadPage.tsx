import { PageStub } from '@/components/layout/PageStub';

export function UploadPage() {
  return (
    <PageStub
      eyebrow="Welcome back, Alex"
      title="Find the roles actually worth your time."
      description="Drop in your résumé and Coffee Grinder reads every posting for you — scoring how well each one fits, so you spend energy only on the matches that matter."
      planned="Drag-and-drop résumé upload (empty + parsed states), the parsed-profile summary, and a primary CTA into run setup."
    />
  );
}
