import { PageStub } from '@/components/layout/PageStub';

export function RunSetupPage() {
  return (
    <PageStub
      eyebrow="Start a grind"
      title="Set up your grind"
      description="Tell us what you’re after. We’ll fetch matching postings and score each one against your résumé."
      planned="Job title/keywords, location, remote toggle, source selector (auto-fetch vs paste), the “how many jobs?” slider, and a Start grinding CTA."
    />
  );
}
